#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

export GOBIN="${ASDF_INSTALL_PATH}/bin"
go install sigs.k8s.io/controller-tools/cmd/{controller-gen,helpgen,type-scaffold}@v"${ASDF_INSTALL_VERSION}"
